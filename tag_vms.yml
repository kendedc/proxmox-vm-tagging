---
# Reconcile Proxmox guest notes from the Excel source of truth
# Everything runs against the Proxmox API from the controller — no SSH to guests
- name: Reconcile Proxmox VM metadata
  hosts: localhost
  connection: local
  gather_facts: true
  become: false

  roles:
    - role: proxmox_tagging
